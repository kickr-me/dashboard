= form_with(model: match, local: true) do |form|
  = form.select :team_a_id, MatchTeam.all.collect {|x| [x.title, x.id]}, {}
  vs
  = form.select :team_b_id, MatchTeam.all.collect {|x| [x.title, x.id]}, {}
  - if match.errors.any?
    #error_explanation
      %h2
        = pluralize(match.errors.count, "error")
        prohibited this match from being saved:
      %ul
        - match.errors.full_messages.each do |message|
          %li= message
  .actions
    = form.submit
